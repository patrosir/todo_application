<html>

<body>
  <main class="TodoPageMain">
    <article>
      <h1 class="TodoPage-title"> My Todo-List</h1>
      <%= form_with(url: "/todos", method: "post", class: "AddTodo") do %>
          <%=text_field_tag ('todo_text', nil, class: "AddTodo-text",placeholder:"What's next?")%><%end%>

          <input class="AddTodo-date" type="date" name="due_date">
          <input class="AddTodo-button" type="submit" value="Add">
      <% end %>

      <%= render partial: "todo-section", locals: {
                   section_title: "Overdue",
                   todos: Todo.overdue,
                   show_due_date: true,
                   is_overdue: true,
                 } %>

      <%= render partial: "todo-section", locals: {
                   section_title: "Due Today",
                   todos: Todo.due_today,
                   show_due_date: false,
                   is_overdue: false,
                 } %>

      <%= render partial: "todo-section", locals: {
                   section_title: "Due Later",
                   todos: Todo.due_later,
                   show_due_date: true,
                   is_overdue: false,
                 } %>

    </article>
  </main>

</body>

</html>
